package com.week1.algoritms

object TestScala {
  def main(args: Array[String]): Unit = {
//    val arr = Array(2,4,2,6,4,2)
//    val sortedArr = new InsertSortScala(arr).sort()
//    sortedArr.foreach(i => print(i))
  }
}
